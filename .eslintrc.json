{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "rule-name": "off", // Replace "rule-name" with the ESLint rule you want to disable
    "another-rule": "warn" // Example of setting a rule to show warnings instead of errors
  }
}
